


namespace Emby.CycleImages
{
    public class Learning
    {
        private void learn()
        {
            /*
            long manualid = 382;
            //long manualid = 67;
            var ch = LibraryManager.QueryItems(new InternalItemsQuery
            {

                CollectionIds = new[] { manualid },
                DtoOptions = new DtoOptions(true)
                //Recursive = true
            });
            string user = "Nicholas Bird";
            User user1 = _userManager.GetUserByName(user.AsSpan());
            var queryList = new InternalItemsQuery(user1)
            {
                Recursive= true,
                ParentIds = new[] { manualid },
                DtoOptions = new DtoOptions(true),
                
            };

            */
        }
    }

    
}
